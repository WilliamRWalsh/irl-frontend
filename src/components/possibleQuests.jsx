import React, { Component } from 'react';
import { Accordion } from 'react-bootstrap';

class PossibleQuests extends Component {
  state = {}
  render() {
    return (
      <React.Fragment>
        <h3>Available Quests</h3>
        <Accordion>

        </Accordion>
      </React.Fragment>
    );
  }
}

export default PossibleQuests;